---
import { cn } from '../../lib/cn';

export interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---
<p
  class={cn(
    'text-xs font-medium uppercase tracking-[0.2em] text-gold',
    className,
  )}
>
  <slot />
</p>
